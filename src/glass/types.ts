/**
 * Type re-exports from the vendored Liquid DOM core, so the rest of `src/glass`
 * has a single import site.
 */
export type { RgbaColor } from '@/vendor/liquid-dom/core/types';
export type { ContainerInit } from '@/vendor/liquid-dom/core/scene';
